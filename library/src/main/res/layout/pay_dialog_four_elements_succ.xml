<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@android:color/transparent"
    android:gravity="center"
    android:orientation="vertical"
    >

    <ImageView
        android:id="@+id/iv_foureelement_img"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:src="@mipmap/pay_img_verify_succ"
        />

    <Button
        android:id="@+id/btn_fourelement_confirm"
        style="@style/UikitButton.Bottom"
        android:layout_width="170dip"
        android:layout_height="40dip"
        android:text="领取“签章”奖励"
        android:textColor="#ff513516"
        android:textSize="16sp"
        android:layout_below="@id/iv_foureelement_img"
        android:layout_marginTop="40dip"
        />

</LinearLayout>